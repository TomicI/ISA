package com.repository.hotel;

import com.model.hotel.CenovnikHotel;
import org.springframework.data.jpa.repository.JpaRepository;

public interface CenovnikHotelRepository extends JpaRepository<CenovnikHotel, Long> {
}
